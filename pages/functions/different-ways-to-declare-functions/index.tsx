import { Box } from '@mui/material';

export default function DifferentWaysToDeclareFunctions() {
  return (
    <Box
      sx={{ display: 'inherit', flexDirection: 'column', maxWidth: '100%' }}
    ></Box>
  );
}
